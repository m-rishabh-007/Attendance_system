# requirements-docker.txt
# Docker deployment dependencies - Python 3.11 REQUIRED
# Optimized for Raspberry Pi (ARM architecture) and headless deployment
#
# ⚠️  CRITICAL: These versions are locked for stability
# DO NOT upgrade without testing

# Core numerical libraries
# NumPy MUST be <2.0 for MediaPipe compatibility
numpy>=1.23.0,<2.0

# Computer vision
# Use 4.10.0.84 for stability with NumPy 1.x
opencv-python-headless==4.10.0.84

# YAML configuration
PyYAML>=6.0

# TFLite Runtime (lightweight inference - Python 3.11 compatible)
tflite-runtime>=2.14.0

# MediaPipe for face alignment (Phase 2)
mediapipe>=0.10.21

# Ultralytics utilities (lightweight, no training dependencies)
ultralytics>=8.0.0

# Note: For development with model training/export, use requirements.txt instead
# Docker image is optimized for inference only (smaller size, faster startup)
